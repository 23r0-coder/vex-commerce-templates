import type { CSSProperties } from 'react';

export const primaryButtonStyle: CSSProperties = {
  borderRadius: 'var(--btn-radius)',
  textTransform: 'var(--btn-text-transform)' as CSSProperties['textTransform']
};
